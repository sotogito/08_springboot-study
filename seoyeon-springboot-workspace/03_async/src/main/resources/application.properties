spring.application.name=03_async

    # classpath? ????? schema.sql(DDL)? data.sql(DML)? ?????
    spring.sql.init.mode=always

    # DB ??
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
    spring.datasource.url=jdbc:mysql://localhost:3306/bootdb
    spring.datasource.username=podoseee
    spring.datasource.password=podoseee

    # MyBatis ??
    mybatis.mapper-locations=mappers/*.xml
    mybatis.type-aliases-package=com.podoseee.async.dto
